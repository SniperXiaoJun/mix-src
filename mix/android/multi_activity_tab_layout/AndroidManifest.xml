<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.mix"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <!-- 默认 -->
    <!-- -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
   

    <!-- 全屏 -->
    <!--  
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        
        -->

        <!-- 无标题 -->
        <!--
      <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar">
        -->

        <activity
            android:name="com.android.mix.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.tab.FragmentAndFragmentManagerActivity" />
        <activity android:name=".view.tab.ViewPagerAndPagerTitleStripActivity" />
        <activity android:name=".view.tab.ViewPagerAndPageAdapterActivity" />
        <activity android:name=".view.tab.ViewPagerAndFragmentAndFragmentPagerAdapterActivity" />
        <activity android:name=".view.tab.TabHostAndTabWidgetActivity" />
        <activity android:name=".view.tab.MainFragmentActivity" />
        <activity android:name=".view.tab.MixTabActivity" />
        
        
    </application>

</manifest>